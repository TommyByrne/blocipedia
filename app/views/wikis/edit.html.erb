<%= simple_form_for @wiki do |f| %>
  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :body %>
  </div>
  <div class="form-actions">
    <%= f.button :submit, "Publish Wiki", class: 'btn btn-success' %>
  </div>
<% end %>